# birdscope

Android app for recording and observing bird songs.

**Status:** restart with project-memory approach.
Previous work preserved on branch `legacy`: pull from there as needed.

## Memory

Project state lives in `memory/` — start at `memory/root.json`.
Format described in protocols repo: `project-memory.md`.
